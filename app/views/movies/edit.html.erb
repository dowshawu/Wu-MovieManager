<h1>Editing Movie</h1>

<%= render 'form' %>

<%= link_to 'Show', {:action => 'show', :id => params[:id], :user_id => @user.id } %> |
<%= link_to 'Back', {:action => 'index', :user_id => @user.id } %>
